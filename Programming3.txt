#include<stdio.h>

struct book
{
    char title[20];
    float price;
};
void main()
{
    struct book b,*ptr;
    ptr=&b;
    printf("enter the title of book: ");
    gets(b.title);
    printf("enter the peice of book: ");
    scanf("%f",&ptr->price);
    printf("The name of the book is: ");
    puts(b.title);
    printf(The price is: %f",b.price));
}